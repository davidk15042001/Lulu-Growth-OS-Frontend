import { useEffect, useState } from 'react';
import { CheckCircle2, FileText, XCircle } from 'lucide-react';
import { requestApi } from '../../api/client';
import { useParams } from 'react-router-dom';
import { useTranslation } from '../../i18n/GlobalLanguageSwitcher';

type SellerProfile = { companyName?: string | null; countryRegion?: string | null; taxId?: string | null; address?: string | null; legalForm?: string | null; legalRepresentative?: string | null; phoneNumber?: string | null; bankAccountNumber?: string | null; bankOpeningBank?: string | null; bankBranch?: string | null; bankCode?: string | null };
type Document = { type: 'QUOTE' | 'INVOICE'; number: string; status: string; currency: string; versionNumber?: number; validUntil?: string | null; issueDate?: string | null; dueDate?: string | null; subtotal: string; discountTotal: string; shippingTotal: string; taxTotal: string; grandTotal: string; amountPaid?: string; amountDue?: string; sellerProfile?: SellerProfile | null; lines: Array<{ productName: string; quantity: string; quantityUnit?: string | null; unitPrice: string; lineTotal: string }> };

export default function PublicCommercialDocumentPage() {
  const { token } = useParams<{ token: string }>();
  const t = useTranslation();
  const [document, setDocument] = useState<Document | null>(null);
  const [loading, setLoading] = useState(true);
  const [message, setMessage] = useState('');
  const [deciding, setDeciding] = useState(false);

  useEffect(() => {
    if (!token) return;
    void requestApi<Document>({ path: `/public/commercial-documents/${encodeURIComponent(token)}` }).then((result) => setDocument(result.data)).catch(() => setMessage(t('This document is unavailable or the link has expired.'))).finally(() => setLoading(false));
  }, [token, t]);

  const decide = async (decision: 'ACCEPTED' | 'DECLINED') => {
    if (!token || !document || document.type !== 'QUOTE') return;
    setDeciding(true); setMessage('');
    try { await requestApi({ path: `/public/commercial-documents/${encodeURIComponent(token)}/decision`, method: 'POST', body: { decision } }); setDocument({ ...document, status: decision }); setMessage(decision === 'ACCEPTED' ? t('Thank you. The quote was accepted.') : t('The quote was declined.')); } catch { setMessage(t('The decision could not be saved.')); } finally { setDeciding(false); }
  };

  if (loading) return <main className="grid min-h-screen place-items-center bg-[var(--background)] p-6 text-sm text-[var(--muted-foreground)]">{t('Loading document…')}</main>;
  if (!document) return <main className="grid min-h-screen place-items-center bg-slate-100 p-6"><div className="rounded-3xl border border-slate-200 bg-white p-8 text-center shadow-sm"><FileText className="mx-auto mb-3 text-slate-400"/><p className="text-sm text-slate-600">{message || t('Document not found.')}</p></div></main>;
  return <main className="min-h-screen bg-gradient-to-br from-slate-100 via-white to-indigo-50/60 p-4 sm:p-8"><article className="mx-auto max-w-4xl overflow-hidden rounded-3xl border border-slate-200 bg-white text-slate-900 shadow-[0_24px_80px_-40px_rgba(15,23,42,.5)]"><div className="h-1.5 bg-gradient-to-r from-indigo-500 via-violet-500 to-cyan-400"/><header className="border-b border-slate-200 p-6 sm:p-10"><div className="flex flex-wrap items-start justify-between gap-6"><div><p className="text-xs font-semibold uppercase tracking-[.2em] text-indigo-600">{t('Lulu Commerce')}</p><h1 className="mt-2 text-4xl font-semibold tracking-tight">{document.type === 'QUOTE' ? t('Quote') : t('Invoice')}</h1><p className="mt-2 text-sm text-slate-500">{document.number}{document.versionNumber ? ` · ${t('Version')} ${document.versionNumber}` : ''}</p></div><span className="rounded-full border border-indigo-100 bg-indigo-50 px-4 py-2 text-xs font-semibold uppercase tracking-wide text-indigo-700">{document.status}</span></div></header><section className="space-y-8 p-6 sm:p-10"><div className="grid grid-cols-2 gap-4 sm:grid-cols-4"><Summary label={t('Subtotal')} value={`${document.subtotal} ${document.currency}`}/><Summary label={t('Discount')} value={`${document.discountTotal} ${document.currency}`}/><Summary label={t('Shipping')} value={`${document.shippingTotal} ${document.currency}`}/><Summary label={t('Total')} value={`${document.grandTotal} ${document.currency}`} accent/></div><div className="overflow-x-auto rounded-2xl border border-slate-200"><table className="w-full min-w-[560px] text-left text-sm"><thead className="bg-slate-900 text-xs font-semibold uppercase tracking-wide text-white"><tr><th className="px-5 py-4">{t('Product')}</th><th className="px-5 py-4">{t('Quantity')}</th><th className="px-5 py-4 text-right">{t('Unit price')}</th><th className="px-5 py-4 text-right">{t('Amount')}</th></tr></thead><tbody className="divide-y divide-slate-200">{document.lines.map((line, index) => <tr key={`${line.productName}-${index}`}><td className="px-5 py-4 font-semibold">{line.productName}</td><td className="px-5 py-4 text-slate-600">{line.quantity} {line.quantityUnit || ''}</td><td className="px-5 py-4 text-right text-slate-600">{line.unitPrice} {document.currency}</td><td className="px-5 py-4 text-right font-semibold">{line.lineTotal} {document.currency}</td></tr>)}</tbody></table></div>{document.type === 'QUOTE' && ['SENT','VIEWED'].includes(document.status) ? <div className="flex flex-wrap gap-3"><button type="button" disabled={deciding} onClick={() => void decide('ACCEPTED')} className="inline-flex items-center gap-2 rounded-xl bg-emerald-600 px-5 py-3 text-sm font-semibold text-white shadow-sm transition hover:bg-emerald-700 disabled:opacity-50"><CheckCircle2 size={16}/> {t('Accept quote')}</button><button type="button" disabled={deciding} onClick={() => void decide('DECLINED')} className="inline-flex items-center gap-2 rounded-xl border border-rose-200 px-5 py-3 text-sm font-semibold text-rose-700 transition hover:bg-rose-50 disabled:opacity-50"><XCircle size={16}/> {t('Decline')}</button></div> : null}{message ? <p role="status" className="text-sm text-slate-500">{message}</p> : null}{document.sellerProfile ? <SellerFooter kind={document.type} profile={document.sellerProfile} t={t}/> : null}</section></article></main>;
}

function Info({ label, value }: { label: string; value: string }) { return <div><p className="text-xs text-[var(--muted-foreground)]">{label}</p><p className="mt-1 font-medium">{value}</p></div>; }
function Summary({ label, value, accent = false }: { label: string; value: string; accent?: boolean }) { return <div className={`rounded-2xl p-4 ${accent ? 'bg-indigo-50 ring-1 ring-indigo-100' : 'bg-slate-50'}`}><p className="text-xs font-semibold uppercase tracking-wide text-slate-500">{label}</p><p className={`mt-2 font-semibold ${accent ? 'text-xl text-indigo-700' : 'text-slate-900'}`}>{value}</p></div>; }
function SellerFooter({ kind, profile, t }: { kind: Document['type']; profile: SellerProfile; t: ReturnType<typeof useTranslation> }) { return <footer className="mt-10 border-t-2 border-[var(--border)] pt-5 text-sm"><h2 className="text-xs font-semibold uppercase tracking-wide text-[var(--muted-foreground)]">{t('Seller')}</h2><p className="mt-1 font-semibold">{profile.companyName || '—'}</p><p className="text-[var(--muted-foreground)]">{[profile.address, profile.countryRegion].filter(Boolean).join(', ') || '—'}</p><div className="mt-2 grid gap-x-4 gap-y-1 text-xs text-[var(--muted-foreground)] sm:grid-cols-2"><span>{t('Tax ID')}: {profile.taxId || '—'}</span><span>{t('Legal form')}: {profile.legalForm || '—'}</span><span>{t('Legal representative')}: {profile.legalRepresentative || '—'}</span><span>{t('Phone number')}: {profile.phoneNumber || '—'}</span></div>{kind !== 'QUOTE' ? <div className="mt-3 text-xs text-[var(--muted-foreground)]"><p className="font-semibold">{t('Bank details')}</p><p>{profile.bankOpeningBank || '—'} · {profile.bankBranch || '—'}</p><p>{t('Account')}: {profile.bankAccountNumber || '—'} · {t('Bank code')}: {profile.bankCode || '—'}</p></div> : null}</footer>; }
