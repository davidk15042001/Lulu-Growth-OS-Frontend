import { useCallback, useEffect, useState } from 'react';
import { AlertTriangle, Globe2, Inbox, RefreshCw, Search, ShieldCheck } from 'lucide-react';
import { requestApi, getFriendlyErrorMessage } from '../../api/client';
import { useLuluApp } from '../../api/LuluAppContext';

type Row={id:string;workspaceId:string;workspaceName?:string;subject:string|null;status:string;handlingMode:string;channelDisplayName?:string;identityDisplayName?:string;lastMessageAt:string|null;};
type Routing={id:string;channel_type:string;identity_display_name:string;message_preview:string|null;confidence:string;reason:string|null;created_at:string;};
type Channel={id:string;channelType:string;displayName:string;status:string;identities:Array<{id:string;workspaceId:string|null;status:string}>};
export default function AdminOmniChannelPage(){
  const {currentUser}=useLuluApp(); const [rows,setRows]=useState<Row[]>([]); const [routing,setRouting]=useState<Routing[]>([]); const [channels,setChannels]=useState<Channel[]>([]); const [tab,setTab]=useState<'inbox'|'routing'|'channels'>('inbox'); const [query,setQuery]=useState(''); const [loading,setLoading]=useState(true); const [error,setError]=useState('');
  const load=useCallback(async()=>{setLoading(true);setError('');try{const [inbox,queue,channelResult]=await Promise.all([requestApi<{items:Row[]}>({path:`/admin/omnichannel/conversations?limit=200${query?`&search=${encodeURIComponent(query)}`:''}`}),requestApi<Routing[]>({path:'/admin/omnichannel/routing'}),requestApi<Channel[]>({path:'/admin/omnichannel/channels'})]);setRows(inbox.data.items);setRouting(queue.data);setChannels(channelResult.data);}catch(cause){setError(getFriendlyErrorMessage(cause,'Admin OmniChannel konnte nicht geladen werden.'));}finally{setLoading(false);}},[query]);
  useEffect(()=>{if(currentUser?.role==='admin')void load();},[currentUser,load]);
  return <main className="page-frame min-h-screen bg-[var(--background)] px-4 py-6 sm:px-6 lg:px-8"><div className="mx-auto max-w-[1400px] space-y-6"><nav className="flex gap-2 overflow-x-auto rounded-2xl border border-[var(--border)] bg-[var(--card)] p-2 text-sm"><a href="/admin" className="shrink-0 rounded-xl px-3 py-2 hover:bg-[var(--secondary)]">Admin Übersicht</a><a href="/admin/omnichannel" className="shrink-0 rounded-xl bg-[var(--foreground)] px-3 py-2 text-[var(--background)]">OmniChannel</a></nav><header className="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><p className="eyebrow">Lulu Platform Operations</p><h1 className="text-3xl font-semibold tracking-tight">OmniChannel Admin</h1><p className="mt-1 text-sm text-[var(--muted-foreground)]">Globale Kommunikation – jeder Zugriff ist capability-geschützt und wird protokolliert.</p></div><button onClick={()=>void load()} className="inline-flex items-center gap-2 self-start rounded-full border border-[var(--border)] px-4 py-2 text-sm hover:bg-[var(--secondary)]"><RefreshCw size={15}/> Aktualisieren</button></header>{error?<div className="rounded-xl border border-red-200 bg-red-50 px-4 py-3 text-sm text-red-800">{error}</div>:null}<div className="flex flex-wrap gap-2"><button onClick={()=>setTab('inbox')} className={`inline-flex items-center gap-2 rounded-full px-4 py-2 text-sm ${tab==='inbox'?'bg-[var(--foreground)] text-[var(--background)]':'border border-[var(--border)]'}`}><Inbox size={15}/> Globaler Posteingang ({rows.length})</button><button onClick={()=>setTab('routing')} className={`inline-flex items-center gap-2 rounded-full px-4 py-2 text-sm ${tab==='routing'?'bg-[var(--foreground)] text-[var(--background)]':'border border-[var(--border)]'}`}><AlertTriangle size={15}/> Routing-Ausnahmen ({routing.length})</button><button onClick={()=>setTab('channels')} className={`inline-flex items-center gap-2 rounded-full px-4 py-2 text-sm ${tab==='channels'?'bg-[var(--foreground)] text-[var(--background)]':'border border-[var(--border)]'}`}><Globe2 size={15}/> Channel Health ({channels.length})</button></div>{tab==='inbox'?<section className="overflow-hidden rounded-3xl border border-[var(--border)] bg-[var(--card)]"><div className="flex items-center gap-3 border-b border-[var(--border)] p-4"><Search size={17} className="text-[var(--muted-foreground)]"/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Workspace, Konversation oder Kanal suchen…" className="w-full bg-transparent text-sm outline-none"/><ShieldCheck size={17} className="text-emerald-600"/></div>{loading?<p className="p-8 text-sm text-[var(--muted-foreground)]">Wird geladen…</p>:rows.length===0?<div className="p-12 text-center text-sm text-[var(--muted-foreground)]"><Globe2 className="mx-auto mb-2"/>Keine globalen Konversationen gefunden.</div>:<div className="divide-y divide-[var(--border)]">{rows.map(row=><div key={row.id} className="grid gap-3 px-5 py-4 md:grid-cols-[minmax(0,1fr)_180px_160px] md:items-center"><div className="min-w-0"><p className="truncate font-medium">{row.subject||'Neue Anfrage'}</p><p className="mt-1 truncate text-xs text-[var(--muted-foreground)]">{row.channelDisplayName} · {row.identityDisplayName}</p></div><div><p className="text-xs text-[var(--muted-foreground)]">Workspace</p><p className="truncate text-sm font-medium">{row.workspaceName||row.workspaceId}</p></div><div className="text-xs"><span className="rounded-full bg-[var(--secondary)] px-2 py-1">{row.handlingMode}</span><span className="ml-2 text-[var(--muted-foreground)]">{row.status}</span></div></div>)}</div>}</section>:tab==='routing'?<section className="overflow-hidden rounded-3xl border border-[var(--border)] bg-[var(--card)]">{loading?<p className="p-8 text-sm text-[var(--muted-foreground)]">Wird geladen…</p>:routing.length===0?<div className="p-12 text-center text-sm text-[var(--muted-foreground)]"><ShieldCheck className="mx-auto mb-2 text-emerald-600"/>Keine ungelösten Routing-Ausnahmen.</div>:<div className="divide-y divide-[var(--border)]">{routing.map(item=><div key={item.id} className="grid gap-3 px-5 py-4 md:grid-cols-[140px_minmax(0,1fr)_130px]"><div className="text-xs"><p className="font-medium">{item.channel_type}</p><p className="text-[var(--muted-foreground)]">{item.identity_display_name}</p></div><div><p className="text-sm">{item.message_preview||'Keine Vorschau'}</p><p className="mt-1 text-xs text-[var(--muted-foreground)]">{item.reason||'Workspace konnte nicht deterministisch aufgelöst werden'}</p></div><span className="h-fit justify-self-start rounded-full bg-amber-100 px-2 py-1 text-xs text-amber-900">{item.confidence}</span></div>)}</div>}</section>:<section className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{channels.map(channel=><div key={channel.id} className="rounded-3xl border border-[var(--border)] bg-[var(--card)] p-5"><div className="flex items-center justify-between"><h2 className="font-semibold">{channel.displayName}</h2><span className={`rounded-full px-2 py-1 text-xs ${channel.status==='ACTIVE'?'bg-emerald-100 text-emerald-800':'bg-amber-100 text-amber-900'}`}>{channel.status}</span></div><p className="mt-2 text-sm text-[var(--muted-foreground)]">{channel.identities.length} Identitäten · Status wird nicht fingiert.</p></div>)}</section>}</div></main>;
}
